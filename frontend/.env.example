# URL de l'API. Laisser vide en développement : le proxy Vite redirige /api
# vers http://localhost:3000, ce qui évite le CORS et fait voyager le cookie de
# refresh sur la même origine.
VITE_API_URL=/api
